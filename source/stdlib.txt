
uint64 strlen(uint8[] str) {
	uint64 index = 0;
	while (str[index] != 0) {
		index++;	
	}
	
	return index;
}

void print(uint8[] str) {
	uint64 len = strlen(str);

	asm("push rax");
	asm("mov rax, 1");
	asm("mov rdi, 1");
	asm("mov rsi, r8");
	asm("pop rdx");	
	asm("syscall");
	
	return;
}

uint64* malloc(uint64 bytes) {
	asm("mov rax, 9");
	asm("mov rdi, 0");
	asm("mov rsi, r8");
	asm("mov rdx, 3");
	asm("mov r10, 34");
	asm("mov r8d, -1");
	asm("mov r9, 0");
	asm("syscall");

	return;
}
