import stdlib;

uint8 main() {
	uint64 previous = 0;
	uint64 current = 1;
	for (uint8 i = 1; i < 50; i++) {
		printf("%d\n", current);
		uint64 save_current = current;
		current += previous;
		previous = save_current;
	}

	return 0;
}
